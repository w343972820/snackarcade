# build output
dist/
# ad-hoc verification builds: `astro build --outDir dist-adson`, dist-verify, ...
dist-*/
.astro/

# dependencies
node_modules/

# generated: local game mirror (source of truth is games-src/)
public/play/

# temporary aside used while re-mirroring (lives outside public/)
.play-stale-*

# publish cache
.publish-cache.json

# scratch artefacts from the engineer (upstream code set aside during the
# 18-game integration pass; kept out of git so the working tree stays clean)
.removed-*
.download-meta.json
.download-tmp/

# env
.env
.env.local
.env.production

# logs
*.log
npm-debug.log*

# editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
